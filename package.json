{
  "name": "mpcdi-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-based viewer and validator for MPCDI projection packages (warp + blend) using WebGL2.",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^8.1.5"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
