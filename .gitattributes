# Normalize line endings in the repo to LF; check out platform-native.
* text=auto eol=lf

# Windows scripts must keep CRLF on disk.
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary assets — never diff or convert.
*.png binary
*.jpg binary
*.pfm binary
*.mpcdi binary
*.mp4 binary
